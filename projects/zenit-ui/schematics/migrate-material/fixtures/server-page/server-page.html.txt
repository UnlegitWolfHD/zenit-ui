<h2 mat-dialog-title>Server bearbeiten</h2>

<mat-dialog-content>
  <form [formGroup]="form" (ngSubmit)="save()">
    <mat-form-field appearance="outline">
      <mat-label>Name</mat-label>
      <mat-icon matPrefix>dns</mat-icon>
      <input matInput formControlName="name" />
      <button
        mat-icon-button
        matSuffix
        type="button"
        (click)="form.controls.name.reset()"
        matTooltip="Feld leeren"
      >
        <mat-icon>close</mat-icon>
      </button>
      <mat-error *ngIf="form.controls.name.hasError('required')">Gib einen Namen ein.</mat-error>
    </mat-form-field>

    <mat-form-field appearance="outline">
      <mat-label>Standort</mat-label>
      <mat-select formControlName="region">
        <mat-option *ngFor="let region of regions" [value]="region.id">{{ region.name }}</mat-option>
      </mat-select>
    </mat-form-field>
  </form>

  <div class="toolbar">
    <button
      mat-stroked-button
      color="primary"
      type="button"
      [disabled]="loading"
      (click)="reload()"
      matTooltip="Liste neu laden"
      matTooltipPosition="above"
    >
      <mat-icon>refresh</mat-icon>
      Aktualisieren
    </button>
    <button mat-icon-button [matMenuTriggerFor]="menu" aria-label="Weitere Aktionen">
      <mat-icon>more_vert</mat-icon>
    </button>
    <mat-menu #menu="matMenu">
      <button mat-menu-item (click)="exportCsv()">
        <mat-icon>download</mat-icon><span>Als CSV exportieren</span>
      </button>
    </mat-menu>
  </div>

  @if (loading) {
    <mat-spinner diameter="32"></mat-spinner>
  } @else {
    <table mat-table [dataSource]="servers" class="servers">
      <ng-container matColumnDef="name">
        <th mat-header-cell *matHeaderCellDef>Name</th>
        <td mat-cell *matCellDef="let server">
          <mat-icon class="state" [class.state--on]="server.online" aria-hidden="true">{{
            server.online ? 'check_circle' : 'cancel'
          }}</mat-icon>
          {{ server.name }}
        </td>
      </ng-container>
      <ng-container matColumnDef="tags">
        <th mat-header-cell *matHeaderCellDef>Tags</th>
        <td mat-cell *matCellDef="let server">
          <mat-chip-set>
            @for (tag of server.tags; track tag) {
              <mat-chip>{{ tag }}</mat-chip>
            }
          </mat-chip-set>
        </td>
      </ng-container>
      <ng-container matColumnDef="actions">
        <th mat-header-cell *matHeaderCellDef></th>
        <td mat-cell *matCellDef="let server">
          <button
            mat-icon-button
            type="button"
            color="warn"
            (click)="remove(server)"
            [matTooltip]="'Server ' + server.name + ' löschen'"
            [matTooltipDisabled]="!server.deletable"
          >
            <mat-icon>delete</mat-icon>
          </button>
        </td>
      </ng-container>
      <tr mat-header-row *matHeaderRowDef="columns"></tr>
      <tr mat-row *matRowDef="let row; columns: columns"></tr>
    </table>
    <mat-divider></mat-divider>
    <mat-paginator [length]="total" [pageSize]="25" (page)="page($event)"></mat-paginator>
  }
</mat-dialog-content>

<mat-dialog-actions align="end">
  <button mat-button mat-dialog-close type="button">Abbrechen</button>
  <button
    mat-flat-button
    color="primary"
    type="submit"
    [disabled]="form.invalid || saving"
    (click)="save()"
  >
    <mat-spinner *ngIf="saving" diameter="18"></mat-spinner>
    <span *ngIf="!saving">Speichern</span>
  </button>
  <button mat-raised-button color="accent" type="button" (click)="saveAndClose()">
    Speichern und schließen
  </button>
</mat-dialog-actions>
